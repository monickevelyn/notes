export default function Notes() {
    return (
      <>
        
      </>
    );
}

{/* <AddNote value={input} placeholder="Adicione uma nota..." onChange={(e) => setInput(e.target.value)} handleAdd={add} />  */}